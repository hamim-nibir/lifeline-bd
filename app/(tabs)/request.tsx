import { View, Text } from "react-native";

export default function RequestScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-gray-400 text-base">Blood requests</Text>
    </View>
  );
}